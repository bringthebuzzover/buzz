# Archived gaps

Closed gaps live here after they are fixed. Agents discover **open** work from
`gaps/*.md` only — do **not** treat this folder as a backlog.

Each file keeps its original `id` and evidence; `status` is `fixed`.
